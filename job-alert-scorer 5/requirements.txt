# Gmail API
google-auth>=2.28.0
google-auth-oauthlib>=1.2.0
google-auth-httplib2>=0.2.0
google-api-python-client>=2.120.0

# google-auth pulls in cryptography. Pin below 44: newer wheels require a
# newer macOS, which forces a from-source build (needs Rust) on older Macs.
cryptography>=41,<44

# Web fetching + parsing for full job descriptions
requests>=2.31.0
beautifulsoup4>=4.12.0

# Config + secrets
pyyaml>=6.0
python-dotenv>=1.0

# Default AI provider (Anthropic / Claude). Other providers are optional:
#   pip install openai              # for the OpenAI provider
#   pip install google-generativeai # for the Google / Gemini provider
#   pip install groq                # for the Groq provider
anthropic>=0.49.0
